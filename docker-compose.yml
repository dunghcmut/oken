services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: qdrant-local-image-search
    restart: unless-stopped
    ports:
      - "6333:6333"
    volumes:
      - ./qdrant_storage:/qdrant/storage
